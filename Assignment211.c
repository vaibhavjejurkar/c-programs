#include<stdio.h>
int main()
{
    int n,d;
    printf("Enter the number:");
    scanf("%d",&n);
    printf("Enter the digit:");
    scanf("%d",&d);

    printf("resulting number= %d%d",n,d);

    return 0;
}
